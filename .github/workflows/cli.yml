name: silent-cli-execution

on: [push, workflow_dispatch]

jobs:
  job1:
    runs-on: self-hosted
    steps:
    - name: exec-b
      id: b
      run: C:\Users\elgin.ramos\Desktop\RD11\testrun.bat
      shell: cmd
