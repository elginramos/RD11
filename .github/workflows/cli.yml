name: silent-cli-execution

on: [push, workflow_dispatch]

jobs:
  job1:
    runs-on: self-hosted
    steps:
    - name: exec-test18
      id: test18
      run: C:\Users\elgin.ramos\actions-runner\_work\RD11\RD11\13_18\clrlog.bat
      shell: cmd
