name: silent-cli-execution

on: [push, workflow_dispatch]

jobs:
  job1:
    runs-on: self-hosted
    steps:
    - name: exec-test06
      id: test06
      run: C:\Users\elgin.ramos\actions-runner\_work\RD11\RD11\06\test06.bat
      shell: cmd
